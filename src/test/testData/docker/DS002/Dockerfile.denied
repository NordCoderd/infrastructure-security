<warning descr="User should be declared, often by default it is root
Running containers with 'root' user can lead to a container escape situation.">FROM debian:9
RUN apt-get update && apt-get -y install vim && apt-get clean
</warning>