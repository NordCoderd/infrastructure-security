ARG image=ubuntu/apache2
ARG version=latest
<error descr="':latest' tag used
When using image you should use a specific tag to avoid uncontrolled behavior when the image is updated.">FROM $image:$version</error>